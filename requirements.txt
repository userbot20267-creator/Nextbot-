# requirements.txt

# مكتبة تليجرام الأساسية
python-telegram-bot[job-queue]==20.7

# الاتصال بقاعدة بيانات PostgreSQL
psycopg2-binary==2.9.9

# تحميل المتغيرات البيئية من ملف .env
python-dotenv==1.0.0

# طلبات HTTP غير متزامنة (للبحث الخارجي)
aiohttp==3.9.5

# نماذج التحقق من البيانات (Pydantic)
pydantic==1.10.13

# مكتبة requests (اختيارية، قد تستخدم في بعض الأماكن)
requests==2.31.0
